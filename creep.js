class Creep {
    Creep (name, role, group, status, targetID, container) {
        this.name = name;

        this.role = role;

        this.group = group;

        this.status = status;

        this.targetID = targetID;

        this.container = container;
    }
}

module.exports = Creep;